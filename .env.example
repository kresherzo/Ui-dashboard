# ===========================================
# ASR Dashboard Configuration
# ===========================================
# Скопируй этот файл в .env и измени значения

# Redis сервер (куда ASR пишет данные)
# Для локального Redis (тестирование): redis
# Для внешнего Redis: IP адрес или hostname
REDIS_HOST=192.168.1.100
REDIS_PORT=6379

# Redis пароль (опционально)
# Если пусто или не указано - подключение без пароля
# Рекомендуется установить если Redis доступен из сети
REDIS_PASSWORD=
